;; Add hooks to helm mode
;; (defun my-helm-mode-keys ()
;;   "My keys for latex-mode."
;;   (interactive)
;;   (local-set-key (kbd "C-c 1") 'bibtex-completion-insert-key)
;;   )
;; (add-hook 'helm-mode-hook 'my-helm-mode-keys)

